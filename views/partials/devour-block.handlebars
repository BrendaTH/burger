
<li>
	{{burger_name}}
    {{#if devoured}}
        <button type='button' class='button btn' data-id="{{id}}" id='devoured-btn' disabled> Mmmmm...eaten
    {{else}}
        <button type='button' class='button btn btn-primary' data-id="{{id}}" id='devoured-btn'> devour
    {{/if}}
	</button>

</li>